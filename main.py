from tkinter import *
import pandas as pd

bg = "#CDF0EA"

# ==================working area===============
window = Tk()
window.title("higher lower")
window.minsize(width=300, height=400)
window.config(padx=30, pady=30, bg=bg)
window.mainloop()